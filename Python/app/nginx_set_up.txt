# Redirect HTTP → HTTPS
server {
    listen 80;
    server_name siudev.icu www.siudev.icu;
    return 301 https://$host$request_uri;
}

# HTTPS server
server {
    listen 443 ssl;
    server_name siudev.icu www.siudev.icu;

    ssl_certificate /etc/letsencrypt/live/siudev.icu/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/siudev.icu/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "keep-alive";
        proxy_buffering off;
}

    location /static/ {
        alias /root/RAG/app/static/;
    }
}